[package]
name = "actix-pingpong"
version = "0.1.0"
edition = "2021"

[dependencies]
actix = "0.13"
tokio = { version = "1", features = ["time"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
