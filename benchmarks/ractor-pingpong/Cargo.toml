[package]
name = "ractor-pingpong"
version = "0.1.0"
edition = "2021"

[dependencies]
ractor = "0.14"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
