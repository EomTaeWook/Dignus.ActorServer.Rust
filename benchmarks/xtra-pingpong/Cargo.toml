[workspace]

[package]
name = "xtra-pingpong"
edition = "2021"
version = "0.1.0"

[dependencies]
xtra = { version = "0.6", features = ["tokio"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
