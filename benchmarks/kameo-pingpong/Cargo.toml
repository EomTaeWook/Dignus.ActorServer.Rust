[workspace]

[package]
name = "kameo-pingpong"
edition = "2021"
version = "0.1.0"

[dependencies]
kameo = "0.20"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
